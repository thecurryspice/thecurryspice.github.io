<div class="col">
  <a href="{% if tutorial.redirect %}{{ tutorial.redirect }}{% else %}{{ tutorial.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable">
      {% if tutorial.img %}
        {%
          include figure.liquid
          loading="eager"
          path=tutorial.img
          sizes = "250px"
          alt="tutorial thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body">
        <h2 class="card-title">{{ tutorial.title }}</h2>
        <p class="card-text">{{ tutorial.description }}</p>
        <div class="row ml-1 mr-1 p-0">
          {% if tutorial.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ tutorial.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if tutorial.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ tutorial.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
